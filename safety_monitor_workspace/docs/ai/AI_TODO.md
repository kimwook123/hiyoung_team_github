# AI_TODO.md

## Short-term
- [ ] `NoHelmetRule`와 현재 `best.pt` 클래스 체계가 실제로 맞는지 재검증하고, 필요 시 클래스명 매핑 전략을 정리한다.
- [ ] Python 로그 한 줄 포맷과 Flutter 파싱 규약을 문서로 고정해 추후 포맷 변경 리스크를 줄인다.
- [ ] `ui_bridge.json`의 `model_type`, `source_fps`, `log_path`를 Flutter에서 실제 활용할지 여부를 정리한다.
- [ ] `config.py`의 설정값 중 실험 빈도가 높은 항목을 묶어 관리하는 방식이 필요한지 검토한다.
- [ ] `DangerZoneRule`를 실제 GUI 표시나 로그 예시까지 포함해 사용 가능한 상태인지 확인한다.

## Mid-term
- [ ] 새 모델 추가 시 `build_model()` 분기 수동 수정 부담을 줄일 구조가 필요한지 검토한다.
- [ ] 추론 결과 공통 포맷에 클래스명 alias 또는 label mapping 계층이 필요한지 검토한다.
- [ ] Python-Flutter 파일 통신의 실패 상황(파일 잠금, 빈 JSON, 경로 탐색 실패) 대응을 더 명확히 정리한다.
- [ ] 이벤트 로그 외에 JSON/CSV 내보내기가 필요한지 발표/분석 용도 기준으로 검토한다.
- [ ] ROI 침범 감지의 설정/표시 방식을 GUI와 함께 다룰 구조가 필요한지 검토한다.
- [ ] 성능 측정 지표(FPS, 지연 시간, 이벤트 처리 시간) 기록 위치와 표시 방식을 설계한다.

## Long-term
- [ ] 안전모 탐지 외 다른 위험상황 룰을 추가할 때 공통 이벤트 메타데이터 구조를 확장한다.
- [ ] 다중 모델 비교 실험을 위한 입력 재사용, 결과 저장, 비교 UI 구조를 설계한다.
- [ ] 발표/포트폴리오용 시스템 구조도, 데이터 흐름도, 데모 시나리오 문서를 보강한다.
- [ ] 파일 기반 통신을 유지할지, 추후 IPC/소켓 방식으로 확장할지 장단점을 비교 정리한다.
- [ ] 이벤트 히스토리 검색, 필터링, 요약 리포트 기능 필요성을 검토한다.

## Needs Verification
- [ ] 단일 이미지 입력 또는 이미지 배치 입력 지원 코드가 별도로 있는지 확인 필요
- [ ] ONNX 등 YOLO 외 다른 추론 백엔드 지원 계획 또는 코드가 있는지 확인 필요
- [ ] `AppLinkService.readDefaultLink()`가 실제 UI 초기화 흐름에서 쓰이도록 연결할 계획이 있는지 확인 필요
- [ ] `FALL_DOWN`, `FIRE_DETECTED`, `SMOKE_DETECTED`, `LOITERING` 이벤트 타입은 enum만 존재하는지, 미구현 예정 기능인지 확인 필요
- [ ] 루트 `logs/`와 `safety_ai_monitor/logs/`가 동시에 존재하는 운영 이유를 확인할 필요가 있다
- [ ] 학습 스크립트, 데이터셋 관리 규칙, 평가 코드가 별도 저장소에 있는지 확인 필요
