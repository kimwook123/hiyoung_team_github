This folder is no longer the active analysis runtime.

The active inference pipeline now lives under:

- `safety_monitor_client/app/analysis/`

The server keeps only storage and query responsibilities.
